apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata:
  name: portfolio-triggerbinding
  namespace: portfolio
spec:
  params:
    - name: git-ref
      value: $(body.ref)
    - name: git-repo-url
      value: $(body.repository.clone_url)
